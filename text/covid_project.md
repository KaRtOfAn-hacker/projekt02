# ПРОЄКТ: Аналіз даних COVID-19

## Урок 37. EDA та очищення даних

**1. Завантаження та первинний огляд**
* Завантажте датасет COVID-19 у `pandas DataFrame`.
* Виведіть перші 5 рядків та загальну розмірність датасету (`.shape`).
* Перевірте типи даних за допомогою `.dtypes` та виділіть категоріальні й числові колонки.

**2. Очищення та валідація даних**
* Перевірте кількість пропущених значень у кожній колонці (`.isnull().sum()`). Зробіть імп'ютацію (заповнення медіаною для числових).
* Видаліть дублікати з датасету.

**3. Статистичний аналіз**
* Виведіть базові статистики (`.describe()`) для числових колонок.
* Визначте середні значення для `new_cases`, `new_deaths`, `total_cases`.

**4. Візуалізація даних**
* Побудуйте лінійні графіки загальної кількості випадків та смертей за датами для обраних країн (Україна, США, Німеччина).
* Побудуйте Bar-chart порівняння кількості випадків між континентами на останню доступну дату.
* Створіть гістограми розподілу `total_cases` та `total_deaths`.
* Сформулюйте аналітичний висновок (3-5 речень), виділивши ключові інсайти, тренди та можливі аномалії.

## Урок 38. ETL, Feature Engineering та Кореляційний аналіз

**1. Трансформація даних (Feature Engineering)**
* Створіть цільову колонку `high_cases` (бінарна, тип int). Умова: `1`, якщо `new_cases` > 1000; інакше `0`.
* Застосуйте One-Hot Encoding (`pd.get_dummies()`) до колонки `continent`.
* Закодуйте текстову змінну `iso_code` у числовий формат за допомогою LabelEncoder.
* Створіть нові ознаки: `growth_rate_new_cases` та `growth_rate_new_deaths` для кожної країни на основі дати.
* Збережіть оброблений датасет у файл `covid_data_cleaned.csv`.

**2. Кореляційний аналіз**
* Створіть матрицю кореляцій для змінних: `new_cases`, `new_deaths`, `total_cases`, `population`, `gdp_per_capita`.
* Побудуйте теплову карту (`heatmap`) з відображенням коефіцієнтів кореляції.
* Напишіть короткий висновок про те, з чим найбільше корелює кількість нових випадків.

**3. Порівняльний аналіз груп**
* Побудуйте Boxplot залежності `total_deaths_per_million` від `continent`.
* Побудуйте Pair Plot для змінних: `total_cases`, `total_deaths`, `total_vaccinations`, `population`.
* Побудуйте Bar-chart топ-10 країн за `total_cases_per_million` на останню доступну дату.

## Урок 39. Базові моделі Machine Learning (Baseline)

**1. Підготовка до моделювання**
* Відокремте ознаки (X) від цільових змінних (y).
* Цільова змінна для класифікації: `high_cases`.
* Цільова змінна для регресії: `new_cases`.
* Розбийте дані на train та test вибірки (співвідношення 80/20, random_state=42).
* Масштабуйте числові ознаки за допомогою StandardScaler.

**2. Задача класифікації (Baseline)**
* Навчіть базові моделі класифікації: Logistic Regression, Decision Tree, Random Forest, k-NN.
* Оцініть результати за допомогою метрик: **Accuracy, Precision, Recall, F1-score**.
* Побудуйте матриці помилок (confusion matrix) для кожної моделі.

**3. Задача регресії (Baseline)**
* Навчіть базові моделі регресії: Linear Regression, Polynomial Regression, Ridge, Lasso.
* Оцініть результати за допомогою метрик: **MSE, RMSE, MAE, R²**.

## Урок 40. Оптимізація моделей та Ансамблі

**1. Крос-валідація та Гіперпараметри**
* Виконайте 5-кратну крос-валідацію для всіх моделей класифікації.
* Визначте сітку параметрів (`param_grid`) для Random Forest.
* Виконайте оптимізацію гіперпараметрів за допомогою `GridSearchCV`.

**2. Ансамблеві алгоритми**
* Застосуйте алгоритм `GradientBoostingClassifier` для покращення якості класифікації.
* Порівняйте нові метрики з Baseline-показниками.

**3. Інтерпретація моделей**
* Витягніть показники `Feature Importances` з Random Forest та побудуйте Bar-chart найважливіших ознак.
* Побудуйте ROC-криві для всіх моделей класифікації.
* Побудуйте графік залишків (Residuals) для регресійної моделі.
* Використайте SelectKBest для відбору топ-10 найважливіших ознак.
* Проаналізуйте 10 записів з найбільшими помилками прогнозування.

## Аналітичні звіти

**1. Важливість аналізу даних COVID-19 (150-200 слів)**
* Напишіть текст про важливість аналізу COVID-19 даних для суспільства, охорони здоров'я та економіки.

**2. Аналіз графіків та статистики (200-300 слів)**
* Опишіть тренди, які спостерігаються на графіках.
* Які країни/континенти мають найбільше випадків?
* Коли були пікові періоди?
* Опишіть аномалії або несподівані результати.

**3. Аналітичний звіт по машинному навчанні (300-400 слів)**
* Опишіть процес підготовки даних (кодування, масштабування, балансування).
* Опишіть навчені моделі та їх результати.
* Вкажіть, яка модель виявилася найкращою і чому.
* Запропонуйте рекомендації для подальшого покращення.
